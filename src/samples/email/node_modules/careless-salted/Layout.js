var fs = require('fs'),
  path = require('path');

var Careless = require('careless');

module.exports = function Layout(props, context) {

  return (
    <html lang="en">
      <head>
        <title>{props.title}</title>

        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width" />
        <style type="text/css">
          {{raw: fs.readFileSync(path.join(__dirname, 'salted.css'), 'UTF-8')}}
        </style>
      </head>
      <body style="margin: 0; padding: 0;">
        {props.children}
      </body>
    </html>
  )
};